# `ed music reveal`

Resolves a library track and reveals that exact file in Finder.

```text
ed music reveal <track> [--json]
```

JSON reports `action`, the relative `path`, the absolute `file`, and `opened`.

- [`ed music`](./README.md)
- [All command groups](../README.md)
