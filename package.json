{
  "name": "edith",
  "private": true,
  "scripts": {
    "strip-comments": "bun scripts/strip-comments.mjs",
    "check-comments": "bun scripts/strip-comments.mjs --check",
    "selftest": "bun scripts/strip-comments.mjs --selftest",
    "test": "bun test apps/dashboard/tests scripts",
    "lint": "cd apps/dashboard && biome ci .",
    "format": "cd apps/dashboard && biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.16",
    "typescript": "^5.9.3"
  }
}
